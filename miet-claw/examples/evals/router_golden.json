[
  {
    "name": "list recent runs chinese",
    "prompt": "请列出最近的 runs。",
    "expected": {
      "action": "runs"
    }
  },
  {
    "name": "bridge event and neb",
    "prompt": "请把 /tmp/event.json 和 /tmp/neb.txt bridge 成 KMC lookup，到 /tmp/bridge_workdir 并验证一下。",
    "expected": {
      "action": "bridge_kmc_lookup",
      "params": {
        "event_json": "/tmp/event.json",
        "neb_txt": "/tmp/neb.txt",
        "workdir": "/tmp/bridge_workdir",
        "validate": true
      }
    }
  },
  {
    "name": "diagnose latest kmc log plan",
    "mode": "plan",
    "prompt": "请检查最新那个 run 的 KMC 日志并总结一下。",
    "expected": {
      "actions": [
        "inspect",
        "logs"
      ],
      "summarize": true
    }
  },
  {
    "name": "open web console",
    "prompt": "打开 web 控制台。",
    "expected": {
      "action": "open_web",
      "params": {
        "port": 4174
      }
    }
  },
  {
    "name": "moire run seed ensemble ovito",
    "fixtures": {
      "dirs": [
        "{output_dir}/MoRe/Re_0.07/model_4"
      ]
    },
    "prompt": "请直接在本机上跑 MoRe 的 LAMMPS，然后把结果接到 KMC，用 OVITO 可视化，并使用随机种子 4101,4102,4103：{output_dir}/MoRe/Re_0.07/model_4",
    "expected": {
      "action": "moire_run",
      "params": {
        "case_dir": "{output_dir}/MoRe/Re_0.07/model_4",
        "kmc_seeds": [
          4101,
          4102,
          4103
        ],
        "ovito": true
      }
    }
  },
  {
    "name": "moire compare two events",
    "fixtures": {
      "dirs": [
        "{output_dir}/MoRe/Re_0.07/model_4"
      ],
      "files": {
        "{output_dir}/event_a.json": "{}",
        "{output_dir}/event_b.json": "{}"
      }
    },
    "prompt": "请比较这两个 MoRe event 的 barrier，并接着跑 KMC，用 OVITO 可视化，随机种子 4101,4102：{output_dir}/MoRe/Re_0.07/model_4 {output_dir}/event_a.json {output_dir}/event_b.json",
    "expected": {
      "action": "moire_compare",
      "params": {
        "case_dir": "{output_dir}/MoRe/Re_0.07/model_4",
        "event_jsons": [
          "{output_dir}/event_a.json",
          "{output_dir}/event_b.json"
        ],
        "kmc_seeds": [
          4101,
          4102
        ],
        "ovito": true
      }
    }
  },
  {
    "name": "moire diffusion sweep temperatures",
    "fixtures": {
      "dirs": [
        "{output_dir}/MoRe/Re_0.07/model_4"
      ],
      "files": {
        "{output_dir}/event.json": "{}"
      }
    },
    "prompt": "请先用这个 event 做 MoRe 的 LAMMPS barrier，再扫温 700K 800K 900K，算扩散系数与温度的关系，并用 OVITO 可视化，随机种子 4101,4102：{output_dir}/event.json {output_dir}/MoRe/Re_0.07/model_4",
    "expected": {
      "action": "moire_diffusion_sweep",
      "params": {
        "event_json": "{output_dir}/event.json",
        "case_dir": "{output_dir}/MoRe/Re_0.07/model_4",
        "temperatures_k": [
          700.0,
          800.0,
          900.0
        ],
        "kmc_seeds": [
          4101,
          4102
        ],
        "ovito": true
      }
    }
  },
  {
    "name": "compare latest two md kmc runs",
    "prompt": "请比较最新两次 MD→KMC run 的结果。",
    "expected": {
      "action": "compare_runs",
      "params": {
        "run": "latest_two",
        "mode": "md_to_kmc_chain"
      }
    }
  }
]
