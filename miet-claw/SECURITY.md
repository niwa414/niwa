# Security

Do not commit local model API keys, cloud credentials, generated run artifacts, or private simulation data.

The default `.gitignore` excludes local sessions, generated runs, autonomy workspaces, native build outputs, and external engine checkouts.

If you find a security issue, please open a private report through GitHub security advisories when the repository is hosted on GitHub.
