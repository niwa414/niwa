# m26-p2-circuit-load-integration

Purpose: verify that magnetic-load outputs are integrated into regular circuit evidence, not only standalone postprocess.

Run:
- `python tools/run_case.py --case m26-p2-circuit-load-integration --stage all`
