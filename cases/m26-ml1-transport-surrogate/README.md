# m26-ml1-transport-surrogate

Purpose: provide a reproducible ML1 surrogate artifact with model hash, inference config, fallback policy, and OOS error checks.

Run:
- `python tools/run_case.py --case m26-ml1-transport-surrogate --stage all`
